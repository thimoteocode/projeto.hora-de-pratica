# Para acessar a página da API use:
*****************************************************************************
instalar os pacotes contidos em requirements.txt
$ pip install -r requirements.txt

aplicar a migrations
$ python manage.py migrate

start no servidor
localhost:8000/swagger
